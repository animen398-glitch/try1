# Advanced Site Analyzer — Python 3.11+
# Install: pip install -r requirements.txt

# GUI
PyQt5>=5.15.9

# Web management console
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Dynamic traffic interception (headless browser)
playwright>=1.40.0
# After install run:  python -m playwright install chromium

# HTTP / encoding (stdlib-only, no extra deps needed for core modules)
# gzip, zlib, urllib — all included in Python stdlib

# Image Extractor: HTTP fetching + HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0

# Optional: faster HTML parsing for large-scale scraping
# lxml>=5.1.0
