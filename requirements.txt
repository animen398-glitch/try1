# Advanced Site Analyzer — Python 3.11+
# Install: pip install -r requirements.txt

# ── Core (required for the GUI / .exe build) ──────────────────────────────
PyQt5>=5.15.9
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0                  # faster HTML parsing

# ── Optional features ─────────────────────────────────────────────────────
# Enable only if you use these; they bloat / complicate the PyInstaller build
# and are NOT bundled into the .exe (external binaries).
# playwright>=1.40.0         # dynamic traffic analysis; then: python -m playwright install chromium
# yt-dlp>=2024.1.0           # video downloader + Instagram/social media (external CLI)
# ffmpeg                     # external binary; required to merge 4K/1080p video+audio
# fastapi>=0.110.0           # web console (main_orchestrator --web)
# uvicorn[standard]>=0.27.0
# fpdf2>=2.7                 # PDF export of the vulnerability report

# ── Packaging (build-time only) ───────────────────────────────────────────
# pip install pyinstaller     # build: pyinstaller build.spec --clean --noconfirm
