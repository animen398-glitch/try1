# Development / test dependencies.
# Install: pip install -r requirements.txt -r requirements-dev.txt
# Run tests: pytest

pytest>=8.0
ruff>=0.5            # lint gate (pyflakes rules)

# Optional — enable the web-console endpoint tests (otherwise skipped):
fastapi>=0.110.0
httpx>=0.27.0

# PyQt5 (from requirements.txt) is needed — the GUI tests build widgets headless.
