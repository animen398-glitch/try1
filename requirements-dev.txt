# Development / test dependencies.
# Install: pip install -r requirements.txt -r requirements-dev.txt
# Run tests: pytest

# Upper bounds match requirements.txt: cap below the next major (or 1.0 for
# 0.x packages) to keep a known-good range.

pytest>=8.0,<10.0
ruff>=0.5,<1.0      # lint gate (pyflakes rules); 0.x, breaking between minors

# Optional — enable the web-console endpoint tests (otherwise skipped):
fastapi>=0.110.0,<1.0
httpx>=0.27.0,<1.0
# Plain uvicorn (no [standard]) so it installs cleanly across the CI matrix —
# the console only calls uvicorn.run; the app itself needs just fastapi. Present
# here so CI exercises the serve path instead of degrading past it.
uvicorn>=0.27.0,<1.0

# Optional — enable the vuln-report PDF export test (otherwise skipped):
fpdf2>=2.7,<3.0

# The Qt binding (from requirements.txt: PySide6-Essentials + qtpy) is needed —
# the GUI tests build widgets headless under QT_API (conftest pins "pyside6").
