[pytest]
testpaths = tests
addopts = -q
# Tests run Qt headless; conftest.py sets QT_QPA_PLATFORM=offscreen on import.
markers =
    real_operations_db: opt out of auto operations-DB isolation (asserts the real OPERATIONS_DB path).
